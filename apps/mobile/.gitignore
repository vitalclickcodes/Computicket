# Flutter / Dart
.dart_tool/
.packages
build/
.flutter-plugins
.flutter-plugins-dependencies
pubspec.lock

# Platform shells are generated by `flutter create .` on first checkout.
# Keeping them out of source control so the upstream Flutter SDK can
# bump them without polluting our diff.
ios/
android/
web/
linux/
macos/
windows/

# IDE
.idea/
.vscode/
*.iml

# OS
.DS_Store
